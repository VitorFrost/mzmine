name: MZmine v4.0.8 smoothing differential

on:
  workflow_dispatch:
  push:
    branches:
      - "open-offline-main"
      - "agent/v408-smoothing-differential"
    paths:
      - ".github/workflows/v408_smoothing_differential.yml"
      - "datasets/parity/v408_smoothing_inventory.json"
      - "scripts/audit_v408_smoothing_source.py"
      - "scripts/prepare_v408_smoothing_task_probe.py"
      - "scripts/tests/test_prepare_v408_smoothing_task_probe.py"
      - "src/test/java/MZmineSmoothingSideAcceptanceTest.java"
      - "src/main/java/io/github/mzmine/modules/dataprocessing/featdet_smoothing/**"
      - "src/main/java/io/github/mzmine/modules/dataprocessing/featdet_adapchromatogrambuilder/**"
  pull_request:
    branches:
      - "open-offline-main"
    paths:
      - ".github/workflows/v408_smoothing_differential.yml"
      - "datasets/parity/v408_smoothing_inventory.json"
      - "scripts/audit_v408_smoothing_source.py"
      - "scripts/prepare_v408_smoothing_task_probe.py"
      - "scripts/tests/test_prepare_v408_smoothing_task_probe.py"
      - "src/test/java/MZmineSmoothingSideAcceptanceTest.java"
      - "src/main/java/io/github/mzmine/modules/dataprocessing/featdet_smoothing/**"

permissions:
  contents: read

concurrency:
  group: v408-smoothing-${{ github.event_name }}-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  direct-differential:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      ORACLE_COMMIT: 8029f930d28c0447f0acf2bcabef0a79865ad434
      ORACLE_PATH: build/oracle/mzmine-v4.0.8
      INVENTORY: datasets/parity/v408_smoothing_inventory.json
      SOURCE_AUDIT: build/smoothing_parity/v408-smoothing-source-audit.json
      PROBE_SOURCE: src/test/java/io/github/mzmine/modules/dataprocessing/featdet_smoothing/V408SmoothingTaskProbe.java
      PROBE_EVIDENCE: build/smoothing_parity/v408-smoothing-task-probe.json
      DATA_ROOT: build/public_validation_data
      DATASET_ID: zenodo-14001110-banane-30ngml-001
      INPUT_FILE: build/public_validation_data/zenodo/14001110/Banane_30ngmL_001.mzML
      INPUT_SHA256: 2eb189e193925983ddf8348a13a4c96fa7382e4e790665a204251eb036aea77d
      SETTINGS_DATASET_ID: zenodo-14000687-mzmine-processing-settings
      SETTINGS_FILE: build/public_validation_data/zenodo/14000687/MZmine processing settings.xml
      SETTINGS_SHA256: 87845675b0cda32e7ce4c70a2a03f1a8ad465d18d018ede1db57fe61adee074c
      CANDIDATE_REPORT: build/smoothing_parity/open-offline-smoothing-side.json
      ORACLE_REPORT: build/smoothing_parity/v408-smoothing-side.json
      DIFFERENTIAL_REPORT: build/smoothing_parity/v408-open-offline-smoothing-differential.json
      JAVA_TOOL_OPTIONS: -Xmx5g -Djava.util.concurrent.ForkJoinPool.common.parallelism=1
    steps:
      - name: Check out candidate
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 1

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Test smoothing probe preparation
        run: python -m unittest scripts/tests/test_prepare_v408_smoothing_task_probe.py -v

      - name: Set up Java 20
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "20"

      - name: Set up Gradle cache
        uses: gradle/actions/setup-gradle@v4

      - name: Check out frozen v4.0.8 source
        uses: actions/checkout@v4
        with:
          repository: mzmine/mzmine
          ref: 8029f930d28c0447f0acf2bcabef0a79865ad434
          path: build/oracle/mzmine-v4.0.8
          fetch-depth: 1
          persist-credentials: false

      - name: Audit exact smoothing scientific source
        shell: bash
        run: |
          set -euo pipefail
          test "$(git -C "${ORACLE_PATH}" rev-parse HEAD)" = "${ORACLE_COMMIT}"
          mkdir -p build/smoothing_parity
          python scripts/audit_v408_smoothing_source.py \
            --fork-root . \
            --oracle-root "${ORACLE_PATH}" \
            --inventory "${INVENTORY}" \
            --output "${SOURCE_AUDIT}"

      - name: Prepare exact v4.0.8 smoothing task probe
        shell: bash
        run: |
          set -euo pipefail
          rm -f "${PROBE_SOURCE}" "${PROBE_EVIDENCE}"
          python scripts/prepare_v408_smoothing_task_probe.py \
            --checkout "${ORACLE_PATH}" \
            --output "${PROBE_SOURCE}" \
            --evidence "${PROBE_EVIDENCE}"
          test -s "${PROBE_SOURCE}"
          test -s "${PROBE_EVIDENCE}"

      - name: Compile candidate and exact smoothing probe
        shell: bash
        run: |
          set -o pipefail
          ./gradlew compileTestJava --no-daemon --stacktrace \
            2>&1 | tee build/smoothing_parity/v408-smoothing-probe-compile.log

      - name: Download governed public mzML
        run: >-
          python scripts/fetch_public_test_data.py
          --manifest datasets/public_validation_manifest.json
          --dataset "${DATASET_ID}"
          --output-dir "${DATA_ROOT}"
          --timeout 180
          --retries 3

      - name: Download published MZmine settings
        run: >-
          python scripts/fetch_public_test_data.py
          --manifest datasets/public_workflow_manifest.json
          --dataset "${SETTINGS_DATASET_ID}"
          --output-dir "${DATA_ROOT}"
          --timeout 120
          --retries 3

      - name: Verify governed bytes again
        shell: bash
        run: |
          set -euo pipefail
          python scripts/fetch_public_test_data.py --manifest datasets/public_validation_manifest.json \
            --dataset "${DATASET_ID}" --output-dir "${DATA_ROOT}" --verify-existing
          python scripts/fetch_public_test_data.py --manifest datasets/public_workflow_manifest.json \
            --dataset "${SETTINGS_DATASET_ID}" --output-dir "${DATA_ROOT}" --verify-existing

      - name: Execute open-offline smoothing side in isolated JVM
        id: candidate
        continue-on-error: true
        shell: bash
        env:
          MZMINE_SMOOTHING_PARITY_INPUT: ${{ github.workspace }}/${{ env.INPUT_FILE }}
          MZMINE_SMOOTHING_PARITY_SETTINGS: ${{ github.workspace }}/${{ env.SETTINGS_FILE }}
          MZMINE_SMOOTHING_PARITY_SIDE: candidate
          MZMINE_SMOOTHING_PARITY_SIDE_OUTPUT: ${{ github.workspace }}/${{ env.CANDIDATE_REPORT }}
          MZMINE_SMOOTHING_PARITY_INPUT_SHA256: ${{ env.INPUT_SHA256 }}
          MZMINE_SMOOTHING_PARITY_SETTINGS_SHA256: ${{ env.SETTINGS_SHA256 }}
          MZMINE_SMOOTHING_PARITY_CANDIDATE_COMMIT: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          set -o pipefail
          rm -rf build/test-results/test build/reports/tests/test
          ./gradlew test --tests MZmineSmoothingSideAcceptanceTest --no-daemon --stacktrace \
            2>&1 | tee build/smoothing_parity/open-offline-smoothing-side.log
          code=${PIPESTATUS[0]}
          if [[ -f build/test-results/test/TEST-MZmineSmoothingSideAcceptanceTest.xml ]]; then
            cp build/test-results/test/TEST-MZmineSmoothingSideAcceptanceTest.xml \
              build/smoothing_parity/open-offline-smoothing-side-junit.xml
          fi
          exit "${code}"

      - name: Execute frozen v4.0.8 smoothing side in isolated JVM
        id: oracle
        continue-on-error: true
        shell: bash
        env:
          MZMINE_SMOOTHING_PARITY_INPUT: ${{ github.workspace }}/${{ env.INPUT_FILE }}
          MZMINE_SMOOTHING_PARITY_SETTINGS: ${{ github.workspace }}/${{ env.SETTINGS_FILE }}
          MZMINE_SMOOTHING_PARITY_SIDE: oracle
          MZMINE_SMOOTHING_PARITY_SIDE_OUTPUT: ${{ github.workspace }}/${{ env.ORACLE_REPORT }}
          MZMINE_SMOOTHING_PARITY_INPUT_SHA256: ${{ env.INPUT_SHA256 }}
          MZMINE_SMOOTHING_PARITY_SETTINGS_SHA256: ${{ env.SETTINGS_SHA256 }}
          MZMINE_SMOOTHING_PARITY_CANDIDATE_COMMIT: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          set -o pipefail
          rm -rf build/test-results/test build/reports/tests/test
          ./gradlew test --tests MZmineSmoothingSideAcceptanceTest --no-daemon --stacktrace \
            2>&1 | tee build/smoothing_parity/v408-smoothing-side.log
          code=${PIPESTATUS[0]}
          if [[ -f build/test-results/test/TEST-MZmineSmoothingSideAcceptanceTest.xml ]]; then
            cp build/test-results/test/TEST-MZmineSmoothingSideAcceptanceTest.xml \
              build/smoothing_parity/v408-smoothing-side-junit.xml
          fi
          exit "${code}"

      - name: Compare normalized smoothing reports
        if: always()
        shell: bash
        env:
          CANDIDATE_OUTCOME: ${{ steps.candidate.outcome }}
          ORACLE_OUTCOME: ${{ steps.oracle.outcome }}
        run: |
          python - <<'PY'
          import hashlib
          import json
          import os
          import sys
          import xml.etree.ElementTree as ET
          from pathlib import Path

          candidate_path = Path(os.environ["CANDIDATE_REPORT"])
          oracle_path = Path(os.environ["ORACLE_REPORT"])
          output = Path(os.environ["DIFFERENTIAL_REPORT"])

          def junit_problem(path):
            path = Path(path)
            if not path.is_file(): return None
            root = ET.parse(path).getroot()
            for case in root.findall('.//testcase'):
              problem = case.find('failure') or case.find('error')
              if problem is not None:
                return problem.get('message') or (problem.text or '').strip()
            return None

          if not candidate_path.is_file() or not oracle_path.is_file():
            details = {
              "candidate_report_exists": candidate_path.is_file(),
              "oracle_report_exists": oracle_path.is_file(),
              "candidate_outcome": os.environ.get("CANDIDATE_OUTCOME"),
              "oracle_outcome": os.environ.get("ORACLE_OUTCOME"),
              "candidate_junit_problem": junit_problem("build/smoothing_parity/open-offline-smoothing-side-junit.xml"),
              "oracle_junit_problem": junit_problem("build/smoothing_parity/v408-smoothing-side-junit.xml"),
            }
            print(json.dumps(details, indent=2, sort_keys=True))
            raise SystemExit("Smoothing side execution failed before direct comparison")

          candidate = json.loads(candidate_path.read_text(encoding="utf-8"))
          oracle = json.loads(oracle_path.read_text(encoding="utf-8"))
          preconditions = [
            "input_sha256", "published_settings_sha256", "ms1_scan_count",
            "adap_input_feature_count", "adap_input_records_sha256",
            "smoothing_algorithm_class", "smoothing_algorithm_parameters_class",
            "smoothing_suffix", "handle_original"
          ]
          for field in preconditions:
            if candidate.get(field) != oracle.get(field):
              raise SystemExit(f"Smoothing parity precondition differs: {field}")

          candidate_records = candidate["records"]
          oracle_records = oracle["records"]
          equal = candidate_records == oracle_records
          first = None
          for i in range(min(len(candidate_records), len(oracle_records))):
            if candidate_records[i] != oracle_records[i]:
              first = {"index": i, "candidate": candidate_records[i], "oracle": oracle_records[i]}
              break
          if first is None and len(candidate_records) != len(oracle_records):
            first = {"index": min(len(candidate_records), len(oracle_records)), "candidate": None, "oracle": None}

          report = {
            "schema_version": 1,
            "gate_id": "mzmine-v4.0.8-feature-smoothing-direct-differential-v1",
            "candidate_commit": candidate["candidate_commit"],
            "oracle_commit": oracle["oracle_commit"],
            "input_sha256": candidate["input_sha256"],
            "published_settings_sha256": candidate["published_settings_sha256"],
            "ms1_scan_count": candidate["ms1_scan_count"],
            "adap_input_feature_count": candidate["adap_input_feature_count"],
            "adap_input_records_sha256": candidate["adap_input_records_sha256"],
            "smoothing_algorithm_class": candidate["smoothing_algorithm_class"],
            "smoothing_algorithm_parameters_class": candidate["smoothing_algorithm_parameters_class"],
            "smoothing_suffix": candidate["smoothing_suffix"],
            "handle_original": candidate["handle_original"],
            "candidate_output_feature_count": candidate["output_feature_count"],
            "oracle_output_feature_count": oracle["output_feature_count"],
            "candidate_records_sha256": candidate["output_records_sha256"],
            "oracle_records_sha256": oracle["output_records_sha256"],
            "records_equal": equal,
            "first_mismatch": first,
          }
          output.parent.mkdir(parents=True, exist_ok=True)
          output.write_text(json.dumps(report, indent=2, sort_keys=True) + "\n", encoding="utf-8")
          report["report_sha256"] = hashlib.sha256(output.read_bytes()).hexdigest()
          print(json.dumps(report, indent=2, sort_keys=True))
          if not equal:
            sys.exit(1)
          PY

      - name: Remove generated smoothing probe
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          if [[ -f "${PROBE_SOURCE}" ]]; then
            status="$(git status --short -- "${PROBE_SOURCE}" | cut -c1-2)"
            if [[ "${status}" != "??" ]]; then
              echo "generated smoothing probe touched tracked source: ${status}"
              exit 1
            fi
            rm -f "${PROBE_SOURCE}"
          fi

      - name: Remove downloaded public data
        if: always()
        shell: bash
        run: |
          rm -rf "${DATA_ROOT}"
          test ! -e "${DATA_ROOT}"

      - name: Upload smoothing differential evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v408-smoothing-direct-differential-${{ github.run_id }}
          path: |
            build/smoothing_parity/
            build/reports/tests/test/
            build/test-results/test/
          if-no-files-found: warn
          retention-days: 30
